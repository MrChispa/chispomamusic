{
  "schemaVersion": 1,
  "id": "io.github.haripako.omamusic",
  "name": "Omamusic",
  "version": "1.0.0",
  "author": "Hari Vargas",
  "license": "MIT",
  "description": "YouTube Music MPRIS controller with album art, seekable progress bar, shuffle/repeat, and volume in a popup panel",
  "homepage": "https://github.com/haripako/omamusic",
  "kinds": ["bar-widget"],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Omamusic",
    "description": "YouTube Music controller with album art, progress bar, and playback controls",
    "category": "Media",
    "allowMultiple": false,
    "defaultSection": "center",
    "defaults": {
      "playerSource": "Auto",
      "strictBrowserMatch": false,
      "extraPlayerNames": "",
      "showVolume": true,
      "scrollVolume": true,
      "showVisualizer": true,
      "launchCommand": "",
      "quickLinks": "Liked songs|https://music.youtube.com/playlist?list=LM, Library|https://music.youtube.com/library"
    },
    "schema": [
      {
        "key": "playerSource",
        "type": "enum",
        "label": "Player source",
        "options": ["Auto", "Native app", "Browser"],
        "defaultValue": "Auto",
        "description": "Where playback is controlled. Auto prefers a native client and falls back to the browser. Native app gives volume, shuffle and repeat; Browser needs no install and uses its PipeWire stream for volume."
      },
      {
        "key": "strictBrowserMatch",
        "type": "boolean",
        "label": "Strict browser match",
        "description": "Only adopt a browser session while a window titled \"YouTube Music\" is open. More precise, but goes idle if that window switches to another tab (Hyprland only)",
        "defaultValue": false
      },
      {
        "key": "extraPlayerNames",
        "type": "string",
        "label": "Extra player names",
        "description": "Comma-separated MPRIS identities or desktop entries to treat as YouTube Music",
        "defaultValue": ""
      },
      {
        "key": "showVolume",
        "type": "boolean",
        "label": "Show volume slider",
        "description": "Display a volume slider when the player supports it",
        "defaultValue": true
      },
      {
        "key": "scrollVolume",
        "type": "boolean",
        "label": "Scroll to change volume",
        "description": "Adjust volume by scrolling over the bar icon",
        "defaultValue": true
      },
      {
        "key": "showVisualizer",
        "type": "boolean",
        "label": "Show spectrum visualizer",
        "description": "Draw a live audio histogram while playing (requires cava; reflects system audio)",
        "defaultValue": true
      },
      {
        "key": "quickLinks",
        "type": "string",
        "label": "Quick links",
        "description": "Playlist shortcuts shown in the popup, as \"Name|URL\" separated by commas. MPRIS cannot switch playlists, so these open the playlist in your browser instead.",
        "defaultValue": "Liked songs|https://music.youtube.com/playlist?list=LM, Library|https://music.youtube.com/library"
      },
      {
        "key": "launchCommand",
        "type": "string",
        "label": "Launch command",
        "description": "Command run by the play button when nothing is playing. Empty uses the native client if installed, otherwise music.youtube.com",
        "defaultValue": ""
      }
    ]
  }
}
